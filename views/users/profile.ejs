<%- include("../partials/header") %>
<div class="row">
    <div class="col-md-4">
        <h1><%= user.firstname + " " + user.lastname %></h1>
        <div class="img-fluid img-thumbnail">
            <div class="caption">
                <a href="mailto:<%=user.email%>"><%=user.email %></a>
            </div>
                <p>here is the users profile if u want any informtion can get it from here and clarify ur doughts</p>
                <p><a href="/follow/<%= user.id %>"><button class="btn btn-success btn-sm">Follow <%= user.username %></button></a></p>
        </div>
    </div>
    <ul>
    <h3><%= user.firstname + "" + user.lastname %> entertainments :</h3>
        <ul>
            <% entertainment.forEach(function(entertainment) {%>
            <li>
                <a href="/entertainment/<%=entertainment._id %>"><%= entertainment.name%>
                </a>
            </li>
          <% })%>  
        </ul> 
    </ul>
    </div>
</div>

<%- include("../partials/footer") %>

